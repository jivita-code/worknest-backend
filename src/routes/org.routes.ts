// Organization routes (protected - require authentication)
import { Router } from "express";

const router = Router();

// Protected organization routes will go here
// (these routes require authentication via auth middleware)

export default router;